fastapi>=0.110.0
uvicorn>=0.29.0
streamlit>=1.32.0
langchain>=0.3.0
langchain-ollama>=1.1.0
langchain-chroma>=1.1.0
langchain-groq>=1.1.0
langchain-google-genai>=3.1.0
langchain-text-splitters>=0.3.0
chromadb>=1.5.0
# Pin ST + transformers<5: transformers v5 breaks all-MiniLM-L6-v2 embedding scores
# (MRR drifted 0.92 -> 0.775 across machines). Reproducible builds only.
sentence-transformers==3.4.1
pymupdf>=1.25.0
pytest>=8.0.0
pydantic>=2.0.0
numpy>=1.26.0
transformers<5
pandas>=2.0.0
scikit-learn>=1.4.0
httpx>=0.27.0

tenacity>=9.0.0

# Dev tools (optional — for pre-commit)
# pip install pre-commit && pre-commit install
prometheus-client>=0.21.0
